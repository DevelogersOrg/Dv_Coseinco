<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- <record id="action_report_saleorder" model="ir.actions.report">
        <field name="name">Cotización Coseinco</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">sale.report_saleorder</field>
        <field name="report_file">sale.report_saleorder</field>
        <field name="print_report_name">(object.state in ('draft', 'sent') and 'Presupuesto - %s' % (object.name)) or 'Orden - %s' % (object.name)</field>
        <field name="binding_model_id" ref="model_sale_order"/>
        <field name="binding_type">report</field>
    </record> -->

    <record id="action_report_saleorder" model="ir.actions.report">
        <field name="name">Informe Técnico</field>
        <field name="model">account.move</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">dv_repair_equipment.technician_info_report</field>
        <field name="report_file">dv_repair_equipment.technician_info_report</field>
        <field name="print_report_name">'Informe Técnico - %s' % (object.name)</field>
        <field name="binding_model_id" ref="model_account_move"/>
        <field name="binding_type">report</field>
    </record>
</odoo>