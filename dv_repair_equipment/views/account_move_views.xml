<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="account_move_action" model="ir.actions.act_window">
      <field name="name">Facturación</field>
      <field name="type">ir.actions.act_window</field>
      <field name="res_model">account.move</field>
      <field name="view_mode">kanban,tree,form</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            No hay registros para mostrar
        </p>
      </field>
    </record>

    <record id="view_account_move_inherit" model="ir.ui.view">
        <field name="name">account.move.kanban.inherited</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_move_kanban"/>
        <field name="arch" type="xml">
          <xpath expr="//kanban[@class='o_kanban_mobile']" position="replace">
            <kanban class="o_kanban_mobile" sample="1" default_group_by="move_state">

                <!-- Variables a mostrar en el kanban -->
                <field name="move_state" />

                <templates>
                    <t t-name="kanban-box">
                      <div class="oe_kanban_global_click">
                        <div class="oe_kanban_details d-flex flex-column">
                          <strong class="o_kanban_record_title oe_partner_heading"><field name="display_name"/></strong>
                          <div class="o_kanban_tags_section oe_kanban_partner_categories"/>
                          <p>Hola</p>
                        </div>
                      </div>
					          </t>
                </templates>
            </kanban>
          </xpath>
        </field>
    </record>
    
  </odoo>